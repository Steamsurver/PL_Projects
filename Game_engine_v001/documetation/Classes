GameObject - класс, отвечающий, за реализацию большинства объектов на сцене.
    Включает в себя такие компоненты как:
        Абстрактный класс components: имеющий методы, ощие для всех наследников.
        SpriteRender: отвечает за настройки рендеринга, пока что это цвет и текстуры(Sprite).
        Sprite: Класс, который хранит в себе объект тита Texture, и координаты для отрисовки.
        SpriteSheet: Объек, который хранит в себе сразу листы текстур, которые можно в дальнейшем отрисовывать по индексам.
        Texture: хранит в себе путь до текстуры, цвет, и все нужные параметры для дальнейшей отрисовки для GLFW.

Renderer - класс, который хранит в себе пакеты для отрисовки, и в дальнейшем передает отрисовку на сцену.
    RenderBatch: Является пакетом для отрисовки, имеет параметр Z который отвечает за порядок отрисовки,
        так же он отвечает за рендеринг текстур на сцене.


Transform - класс, хранящий в себе 2 2-мерных вектора(положение, масштаб), нужен для оперирования размером и координатами текстур.


